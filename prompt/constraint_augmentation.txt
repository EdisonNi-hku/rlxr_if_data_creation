[System Role]
You are an expert Data Augmentation Specialist for Large Language Model (LLM) training. Your objective is to take simple creative writing prompts and upgrade them into "Complex Instruction-Following Tasks" by adding specific constraints.

[Input Data]
1. [Seed Prompt]: The original, simple instruction.

[Task Instructions]
Your task is to rewrite the [Seed Prompt] by integrating {lower_bound} to {upper_bound} additional constraints based on the [Constraint References].
Constraint References: A list of potential constraints to apply. You may select or combine constraints from it, or use other constraints that deem appropriate.

[Critical Rules]
1. **Preserve Original Intent:** Do NOT delete or alter the core topic, reference texts, or key information from the [Seed Prompt].
2. **Natural Translation:** Do NOT simply list the technical name of the constraint (e.g., do not write "Apply the Lipogram constraint"). Instead, translate the constraint into a clear, natural language instruction (e.g., write "Do not use the letter 'e' in your response").
3. **Logical Consistency:** Before finalizing, verify that the new constraints do not contradict the [Seed Prompt] or each other.
   - *Bad Example:* "Write a 3-sentence story" + "Write a 1000-word story."
   - *Bad Example:* "Start with 'The'" + "Do not use the letter 'T'."
4. **Output Only:** Return ONLY the final modified prompt. Do not include headers, explanations, or "Here is the prompt:" fillers.
5. Add **no more than** {upper_bound} additional constraints!

[Constraint References]
{reference_constraints}
